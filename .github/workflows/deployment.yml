name: Deployment

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'version'
        required: true
      environment:
        description: 'environment'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "will deploy to ${{ github.event.inputs.environment }}!"

